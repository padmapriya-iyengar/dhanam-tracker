# Dhanam guide screenshots

Place privacy-safe WebP screenshots in this directory using the filenames listed in `../index.html`.

The guide deliberately contains capture slots instead of fabricated application screenshots because no authenticated browser or Android emulator session was available during generation.

Use fictional/sample data, hide notification content, and remove personal email addresses, balances, card digits, notes, and authentication details before publishing.
