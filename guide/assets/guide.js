const search=document.querySelector('#guide-search');const sections=[...document.querySelectorAll('main section')];search?.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();sections.forEach(s=>s.classList.toggle('hidden-search',q&&!s.textContent.toLowerCase().includes(q)))});document.querySelector('#menu-button')?.addEventListener('click',()=>document.querySelector('.toc').classList.toggle('open'));document.querySelectorAll('.toc a').forEach(a=>a.addEventListener('click',()=>document.querySelector('.toc').classList.remove('open')));const links=[...document.querySelectorAll('.toc a')];sections.forEach(s=>new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${e.target.id}`))}),{rootMargin:'-20% 0px -70%'}).observe(s));document.querySelectorAll('.shot-placeholder').forEach(slot=>{const name=slot.querySelector('code')?.textContent?.trim();if(!name)return;const img=new Image();img.alt=slot.querySelector('strong')?.textContent||'Dhanam Tracker screenshot';img.style.cssText='display:block;width:100%;height:auto';img.onload=()=>slot.replaceWith(img);img.src=name});
